name: Test Manual Dispatch

on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Show ref
        run: |
          echo "Triggered on ref: ${GITHUB_REF}"
          echo "Event: ${GITHUB_EVENT_NAME}"
          echo "Actor: ${GITHUB_ACTOR}"
